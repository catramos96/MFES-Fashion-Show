class FashionShow
instance variables
  private Events : set of (Event);
  private theme : seq of (char);
  private address : seq of (char);
  private startTime : Resources`Date;
  private endTime : Resources`Date;

end FashionShow
